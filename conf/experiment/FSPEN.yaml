# @package _global_
defaults:
  - override /model: FSPEN
  - override /dset: libriDemand
  - override /loss: mseMag_mstft


model:
  engine:
    train_batch_sz: 12

  predictor:
    _target_: core.Trainer_wGAN_for_fig6.Predictor
