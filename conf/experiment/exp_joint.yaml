# @package _global_
defaults:
  - override /model: joint
  - override /dset: libriDemand

dset:
  train:
    vad: True
  valid:
    vad: True
  vtest:
    vad: True

model:
  engine:
    train_batch_sz: 8
